apiVersion: v1
kind: ConfigMap
metadata:
  name: rdma-devices
  namespace: kube-system
data:
  config.json: |
    {
        "mode" : "sriov",
        #"pfNetdevices": [ "eth0" , "eth1" ]
        "pfNetdevices": [ "ib0" ]
    }
