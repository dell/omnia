# ============================================================================
# GitLab Inventory File
# ============================================================================
# Add the IP/hostname of the server where GitLab will be deployed (hosted mode)
# For external mode, this inventory is not used.
# ============================================================================

[gitlab_server]
# Example: 10.3.0.4 ansible_user=root
# Uncomment and set the target host IP below:
# 10.3.0.4
