---
# =============================================================================
# Image Build Credentials — Ansible Vault encrypted
# =============================================================================
# This file is encrypted with Ansible Vault using the key in
# .image_build_credentials_key (gitignored).
#
# To edit:   ansible-vault edit image_build_credentials.yml --vault-password-file .image_build_credentials_key
# To view:   ansible-vault view image_build_credentials.yml --vault-password-file .image_build_credentials_key
#
# Fields:
#   s3_access_key:  MinIO S3 access key
#   s3_secret_key:  MinIO S3 secret key
# =============================================================================

s3_access_key: ""
s3_secret_key: ""
