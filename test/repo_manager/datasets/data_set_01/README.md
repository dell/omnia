# Dataset: data_set_01

Generated by `datasets/generator/generate_dataset.py` using profile **defaults**.

- Mode: `profile`
- Provenance and artifact hashes: `dataset_manifest.yml`
- Credentials: configured separately on the execution OIM and never generated

## Files

```text
data_set_01/
  input/
    repo_manager_config.yml
    repo_manager_endpoint_config.yml
  dataset_manifest.yml
  README.md
```

## Regenerate

```bash
cd datasets/generator/
python generate_dataset.py data_set_01 defaults --force
```

## Use

Set `dataset: "data_set_01"` and `sync_repo_manager_input: true` in
`test_config.yml`, or select the same values in `test_run_config.yml`.
