# Telemetry FVT — Python Dependencies
# Install: pip install -r requirements.txt
#
# System Dependencies (installed via setup_env.sh):
#   sshpass - Required for PowerScale syslog configuration via SSH
#
# Core test framework
pytest>=9.0
pytest-testinfra>=10.0
pytest-html>=4.0

# YAML parsing
PyYAML>=6.0

# SFM API and forced-menu SSH automation
requests>=2.31
paramiko>=3.4
cryptography>=42.0

# Ansible for playbook execution
ansible-core>=2.15

# Validation (required by telemetry validation module)
jsonschema>=4.17

# omnia_auto shared plugin (local wheel — build from test/plugins/)
../plugins/dist/omnia_auto-1.0.0-py3-none-any.whl
