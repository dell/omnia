# Telemetry FVT — Test Credentials
# This file is auto-encrypted with Ansible Vault on first run.
# Store the vault key in .test_creds.key (auto-generated, git-ignored).

# SSH credentials for target host
ssh:
  username: "root"
  password: ""
  # ssh_key_path: ""

# K8s credentials (if needed for API access)
# k8s:
#   kubeconfig_path: ""
