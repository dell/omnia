# Cluster 3 Configuration
# Connection details for this cluster.
# Sensitive credentials (TARGET_PASS) are stored as GitLab CI/CD masked variables.
CLUSTER_NAME="cluster3"
TARGET_IP=""
TARGET_USER="root"
TARGET_PASS="${CLUSTER3_TARGET_PASS}"  # Resolved from GitLab CI/CD variable (masked)
