# Copyright 2025 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

- name: Validate k8s json
  when: k8s_support is true
  block:
    - name: Load k8s.json
      ansible.builtin.set_fact:
        k8s_packages_json: "{{ lookup('file', base_k8s_json_file) | from_json }}"

    - name: Set fact for dynamic genaration when kubeadm package is kubeadm-1.31.4
      ansible.builtin.set_fact:
        kubeadm_package: "{{ k8s_packages_json['k8s']['cluster'] | selectattr('type', 'equalto', 'tarball') | selectattr('package', 'search', 'kubeadm') | map(attribute='package') | join }}" # noqa: yaml[line-length]
        dynamic_k8s_generation: false

    - name: Set fact for dynamic genaration when kubeadm package is not kubeadm-1.31.4
      ansible.builtin.set_fact:
        dynamic_k8s_generation: true
      when: (kubeadm_package != kubeadm_package_name) or (k8s_version != "1.31.4")

- name: Validate service k8s json
  when: service_k8s_support is true
  block:
    - name: Load service_k8s.json
      ansible.builtin.set_fact:
        service_k8s_packages_json: "{{ lookup('file', base_service_k8s_json_file) | from_json }}"

    - name: Load k8s.json
      ansible.builtin.set_fact:
        service_k8s_kubeadm_package: "{{ service_k8s_packages_json['service_k8s']['cluster'] | selectattr('type', 'equalto', 'tarball') | selectattr('package', 'search', 'kubeadm') | map(attribute='package') | join }}" # noqa: yaml[line-length]
        service_k8s_dynamic_k8s_generation: false

    - name: Set fact for service k8s dynamic genaration when kubeadm package is not kubeadm-1.31.4
      ansible.builtin.set_fact:
        service_k8s_dynamic_k8s_generation: true
      when: (service_k8s_kubeadm_package != kubeadm_package_name) or (service_k8s_version != "1.31.4")

# Append k8s cluster config if k8s_support is true
- name: Add k8s cluster config to cluster_configs
  ansible.builtin.set_fact:
    cluster_configs: "{{ cluster_configs + [k8s_config] }}"
  vars:
    k8s_config:
      name: "k8s"
      version: "{{ k8s_version }}"
      json_file: "{{ dynamic_k8s_json_folder }}/v{{ k8s_version }}/k8s_v{{ k8s_version }}_{{ arch }}.json"
      base_json_file: "{{ project_input_path }}/config/{{ arch }}/{{ cluster_os_type }}/{{ cluster_os_version }}/k8s.json"
      backup_json_file: "{{ dynamic_k8s_json_folder }}/k8s_bkp_{{ arch }}.json"
      run_condition: "{{ k8s_support and dynamic_k8s_generation }}"
      arch: "{{ arch }}"
  when: k8s_support | bool

# Append service_k8s cluster config if service_k8s_support is true
- name: Add service_k8s cluster config to cluster_configs
  ansible.builtin.set_fact:
    cluster_configs: "{{ cluster_configs + [service_k8s_config] }}"
  vars:
    service_k8s_config:
      name: "service_k8s"
      version: "{{ service_k8s_version }}"
      json_file: "{{ dynamic_k8s_json_folder }}/v{{ service_k8s_version }}/service_k8s_v{{ service_k8s_version }}_{{ arch }}.json"
      base_json_file: "{{ project_input_path }}/config/{{ arch }}/{{ cluster_os_type }}/{{ cluster_os_version }}/service_k8s.json"
      backup_json_file: "{{ dynamic_k8s_json_folder }}/service_k8s_bkp_{{ arch }}.json"
      run_condition: "{{ service_k8s_support and service_k8s_dynamic_k8s_generation }}"
      arch: "{{ arch }}"
  when: service_k8s_support | bool
