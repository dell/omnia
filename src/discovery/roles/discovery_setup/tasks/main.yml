# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

# Step 0: Tag validation — reject unsupported or conflicting tags
- name: Set omnia_run_tags from ansible_run_tags
  ansible.builtin.set_fact:
    omnia_run_tags: "{{ ansible_run_tags | default([]) | list }}"
    cacheable: true

- name: Validate tags are supported
  ansible.builtin.fail:
    msg: >-
      Unsupported tag '{{ item }}' specified.
      Supported tags: {{ supported_tags | join(', ') }}
  when:
    - omnia_run_tags | length > 0
    - "'all' not in omnia_run_tags"
    - item not in supported_tags
  loop: "{{ omnia_run_tags }}"

- name: Validate no conflicting tag combinations
  ansible.builtin.fail:
    msg: >-
      Conflicting tags specified: {{ item | join(' + ') }}.
      These tags cannot be used together.
  when:
    - omnia_run_tags | length > 0
    - item | intersect(omnia_run_tags) | length == item | length
  loop: "{{ invalid_tag_combinations }}"

# Step 1: Upgrade guard
- name: Check upgrade lock file
  ansible.builtin.stat:
    path: "{{ upgrade_lock_file }}"
  register: upgrade_lock

- name: Block playbook while upgrade is in progress
  ansible.builtin.fail:
    msg: "{{ upgrade_in_progress_msg }}"
  when:
    - upgrade_lock.stat.exists
    - not (upgrade_mode | default(false) | bool)

# Step 2: Set discovery domain paths
- name: Set discovery project name
  ansible.builtin.set_fact:
    discovery_project_name: "{{ project_name | default('project_default') }}"
    cacheable: true

- name: Set discovery input/output paths
  ansible.builtin.set_fact:
    discovery_input_dir: "{{ omnia_input_dir }}/{{ discovery_project_name }}/discovery"
    discovery_output_dir: "{{ omnia_output_dir }}/{{ discovery_project_name }}/discovery"
    input_project_dir: "{{ omnia_input_dir }}/{{ discovery_project_name }}"
    cacheable: true

# Step 3: Verify input directory
- name: Verify discovery input directory exists
  ansible.builtin.stat:
    path: "{{ discovery_input_dir }}"
  register: discovery_input_stat

- name: Fail if discovery input directory does not exist
  ansible.builtin.fail:
    msg: >-
      Discovery input directory '{{ discovery_input_dir }}' does not exist.
      Please create it and populate with discovery_config.yml and network_spec.yml.
  when: not discovery_input_stat.stat.exists

# Step 4: Ensure output directory
- name: Ensure discovery output directory exists
  ansible.builtin.file:
    path: "{{ discovery_output_dir }}"
    state: directory
    mode: '0755'

# Step 5: Load discovery configuration
- name: Include discovery config file
  ansible.builtin.include_vars:
    file: "{{ discovery_input_dir }}/discovery_config.yml"
  failed_when: false

- name: Set enable_bmc_discovery for credential utility
  ansible.builtin.set_fact:
    enable_bmc_discovery: "{{ true if (discovery_mechanism | default('')) == 'ome' else (enable_bmc_discovery | default(false) | bool) }}"
    cacheable: true

# Step 7: Include common vars
- name: Include common vars
  ansible.builtin.include_vars: "{{ playbook_dir }}/vars/common_vars.yml"
