# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
# Prompt for a single telemetry credential field.
# Called from main.yml in a loop over telemetry_cred_config.mandatory / conditional_mandatory.

# Determine if prompt is needed (skip if value already stored and non-empty)
- name: Reset prompt flags
  ansible.builtin.set_fact:
    _need_username: false
    _need_password: false

- name: Check if username prompt is needed
  ansible.builtin.set_fact:
    _need_username: true
  when:
    - cred_field.username is defined
    - telemetry_loaded_creds[cred_field.username] | default('') | string | length == 0

- name: Check if password prompt is needed
  ansible.builtin.set_fact:
    _need_password: true
  when:
    - cred_field.password is defined
    - telemetry_loaded_creds[cred_field.password] | default('') | string | length == 0

- name: Force username re-prompt when password is empty for same credential
  ansible.builtin.set_fact:
    _need_username: true
  when:
    - cred_field.username is defined
    - cred_field.password is defined
    - _need_password | default(false) | bool
    - not (_need_username | default(false) | bool)

- name: Skip prompt for stored credentials
  ansible.builtin.debug:
    msg: "Credentials already stored for {{ cred_field.username | default(cred_field.password) }} — skipping prompt."
  when: not (_need_username | bool) and not (_need_password | bool)

# Prompt username if needed
- name: Prompt for username
  when:
    - cred_field.username is defined
    - _need_username | default(false) | bool
  block:
    - name: Prompt for credential # noqa name[template]
      ansible.builtin.pause:
        prompt: "[Telemetry] Enter {{ cred_field.get('username', cred_field.get('password')) }}"
      no_log: true
      register: _username_input

    - name: Validate username is not empty for mandatory credential
      ansible.builtin.fail:
        msg: "{{ telemetry_mandatory_warning_msg }} {{ cred_field.get('username') }} cannot be empty."
      when:
        - cred_field.condition is not defined
        - _username_input.user_input | length == 0

# Prompt password if needed (with retry on mismatch)
- name: Prompt for password with retry
  when:
    - cred_field.password is defined
    - _need_password | default(false) | bool
  block:
    - name: Reset password retry counter
      ansible.builtin.set_fact:
        _password_retry_count: 0

    - name: Prompt password with confirmation and retry
      ansible.builtin.include_tasks: prompt_password_with_retry.yml

# Update credential file
- name: Update credential file with new values
  when: (_need_username | default(false) | bool) or (_need_password | default(false) | bool)
  block:
    - name: Decrypt for update
      ansible.builtin.command: >-
        ansible-vault decrypt "{{ telemetry_credential_file.file_path }}"
        --vault-password-file "{{ telemetry_credential_file.vault_path }}"
      changed_when: false
      failed_when: false

    - name: Update username in credential file
      ansible.builtin.lineinfile:
        path: "{{ telemetry_credential_file.file_path }}"
        regexp: "^{{ cred_field.username }}:"
        line: '{{ cred_field.username }}: "{{ _username_input.user_input }}"'
      no_log: true
      when:
        - cred_field.username is defined
        - _need_username | default(false) | bool
        - _username_input.user_input | default('') | length > 0

    - name: Update password in credential file
      ansible.builtin.lineinfile:
        path: "{{ telemetry_credential_file.file_path }}"
        regexp: "^{{ cred_field.password }}:"
        line: '{{ cred_field.password }}: "{{ _password_input.user_input }}"'
      no_log: true
      when:
        - cred_field.password is defined
        - _need_password | default(false) | bool
        - _password_input.user_input | default('') | length > 0

    - name: Reload credentials after update
      ansible.builtin.include_vars:
        file: "{{ telemetry_credential_file.file_path }}"
        name: telemetry_loaded_creds
      no_log: true

    - name: Re-encrypt after update
      ansible.builtin.command: >-
        ansible-vault encrypt "{{ telemetry_credential_file.file_path }}"
        --vault-password-file "{{ telemetry_credential_file.vault_path }}"
      changed_when: false

  rescue:
    - name: Encrypt credential file on failure
      ansible.builtin.shell: |
        set -o pipefail
        if [ -f "{{ telemetry_credential_file.file_path }}" ]; then
          if ! head -n1 "{{ telemetry_credential_file.file_path }}" | grep -q '\$ANSIBLE_VAULT;'; then
            ansible-vault encrypt "{{ telemetry_credential_file.file_path }}" \
              --vault-password-file "{{ telemetry_credential_file.vault_path }}"
          fi
        fi
      changed_when: false

    - name: Fail with cleanup message
      ansible.builtin.fail:
        msg: "{{ telemetry_credential_encrypt_fail_msg }}"
