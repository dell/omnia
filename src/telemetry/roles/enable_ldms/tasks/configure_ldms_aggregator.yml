# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

# =============================================================================
# Configure LDMS aggregator (nersc-ldms-aggr Helm chart)
# =============================================================================
# Prepares the LDMS aggregator Helm chart files on NFS share, including
# machine config, host map, values.yaml, ldmsauth.conf, and munge key.
# The aggregator runs as a StatefulSet in the telemetry namespace.
# =============================================================================

- name: Check if values.yaml already exists
  ansible.builtin.stat:
    path: "{{ ldms_share_path }}/nersc-ldms-aggr/values.yaml"
  register: values_file_exists

- name: Copy LDMS aggregator Helm chart files
  ansible.builtin.copy:
    src: "{{ role_path }}/files/nersc-ldms-aggr"
    dest: "{{ ldms_share_path }}"
    mode: "{{ common_mode }}"
  when: not values_file_exists.stat.exists

- name: Copy LDMS decompose.json
  ansible.builtin.copy:
    src: "{{ role_path }}/files/nersc-ldms-aggr/scripts/decomp.json"
    dest: "{{ ldms_share_path }}/nersc-ldms-aggr/scripts/decomp.json"
    mode: "{{ file_permissions_644 }}"

- name: Generate LDMS machine config
  ansible.builtin.template:
    src: "{{ role_path }}/templates/ldms/ldms_machine_config.json.j2"
    dest: "{{ ldms_share_path }}/nersc-ldms-aggr/ldms_machine_config.json"
    mode: "{{ file_permissions_644 }}"

- name: Generate LDMS host map
  ansible.builtin.template:
    src: "{{ role_path }}/templates/ldms/host_map.slurm-cluster.json.j2"
    dest: "{{ ldms_share_path }}/nersc-ldms-aggr/host_map.slurm-cluster.json"
    mode: "{{ file_permissions_644 }}"

- name: Clean up previous build artifacts before make
  ansible.builtin.file:
    path: "{{ ldms_share_path }}/nersc-ldms-aggr/{{ item }}"
    state: absent
  failed_when: false
  loop:
    - out_dir
    - manifest.yaml
    - values.yaml

- name: Run make command inside LDMS directory
  ansible.builtin.shell: make clean all && make
  changed_when: true
  args:
    chdir: "{{ ldms_share_path }}/nersc-ldms-aggr"

- name: Render values.yaml
  ansible.builtin.template:
    src: "{{ role_path }}/templates/ldms/values.yaml.j2"
    dest: "{{ ldms_share_path }}/nersc-ldms-aggr/values.yaml"
    mode: "{{ file_permissions_644 }}"

- name: Copy munge key from slurm config
  ansible.builtin.copy:
    src: "{{ slurm_config_path }}/munge.key"
    dest: "{{ ldms_share_path }}/munge.key"
    mode: "{{ file_permissions_400 }}"
    remote_src: true

- name: Render ldmsauth.conf to LDMS directory
  ansible.builtin.template:
    src: "{{ role_path }}/templates/ldms/ldmsauth.conf.j2"
    dest: "{{ ldms_share_path }}/ldmsauth.conf"
    mode: "{{ file_permissions_600 }}"

- name: Log LDMS aggregator configuration complete
  ansible.builtin.debug:
    msg: "LDMS aggregator configuration rendered to {{ ldms_share_path }}/nersc-ldms-aggr/"
