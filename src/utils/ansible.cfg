# ansible.cfg for running playbooks from the domain root.
#
# Usage:
#   cd src/utils
#   ansible-playbook playbooks/utils.yml --tags precheck
#
# NOTE: Ansible resolves paths relative to ansible.cfg location, NOT the playbook.
# Since ansible.cfg is at utils/, roles_path is ./roles (same dir).

[defaults]
log_path = /var/log/omnia/utils/utils.log
roles_path = roles:~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles
library = plugins/modules
module_utils = plugins/module_utils
remote_tmp = /tmp/.ansible/tmp/
host_key_checking = false
forks = 5
timeout = 180
executable = /bin/bash
interpreter_python = /usr/bin/python3
deprecation_warnings = false
show_task_path_on_failure = false
collections_path = .:~/.ansible/collections:/usr/share/ansible/collections

[persistent_connection]
command_timeout = 180
connect_timeout = 180

[ssh_connection]
retries = 3
ssh_args = -o ControlMaster=auto -o ControlPersist=60 -o ConnectTimeout=60
