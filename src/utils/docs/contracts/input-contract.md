# Utils Domain -- Input Contract

**Domain**: `utils` | **Collection**: `omnia.utils`

---

## 1. set_pxe_boot_config.yml

**Purpose**: Configuration for PXE boot operations.

**Location**: `input/set_pxe_boot_config.yml`

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `enable_phone_home` | bool | No | `true` | Wait for cloud-init phone-home callbacks |
| `phone_home_pause_minutes` | int | No | `3` | Initial wait before polling (minutes) |
| `phone_home_retries` | int | No | `120` | Max polling iterations |
| `phone_home_delay` | int | No | `15` | Delay between polls (seconds) |
| `restart_host` | bool | No | `true` | Restart host after setting PXE boot |
| `force_restart` | bool | No | `true` | Use ForceRestart vs GracefulRestart |
| `boot_source_override_enabled` | string | No | `continuous` | Boot override mode |
| `boot_source_override_target` | string | No | `pxe` | Boot target |

---

## 2. set_pxe_boot_credentials.yml

**Purpose**: Vault-encrypted BMC credentials.

**Location**: `input/set_pxe_boot_credentials.yml`

**Generated by**: `collect_pxe_credentials` role (interactive prompts)

**Vault key**: `input/.pxe_vault_key`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `bmc_username` | string | Yes | iDRAC/BMC username |
| `bmc_password` | string | Yes | iDRAC/BMC password |

---

## 3. Inventory File

**Purpose**: Define BMC hosts to configure for PXE boot.

**Location**: User-provided (e.g., `inventory.ini`)

**Format**: INI or YAML with `[bmc]` group

### INI Format

```ini
[bmc]
172.16.0.73 admin_ip=172.16.1.50 hostname=node01
172.16.0.74 admin_ip=172.16.1.51 hostname=node02
```

### YAML Format

```yaml
bmc:
  hosts:
    172.16.0.73:
      admin_ip: 172.16.1.50
      hostname: node01
    172.16.0.74:
      admin_ip: 172.16.1.51
      hostname: node02
```

| Host Variable | Type | Required | Description |
|---------------|------|----------|-------------|
| Host (key) | string | Yes | BMC/iDRAC IP address |
| `admin_ip` | string | Yes (for phone-home) | Node's admin network IP |
| `hostname` | string | No | Node hostname |

---

## 4. pxe_mapping_file.csv

**Purpose**: Node-to-BMC mapping for PXE boot operations.

**Location**: `input/project_default/pxe_mapping_file.csv`

**Producer**: Discovery domain or manual creation

| Column | Type | Required | Description |
|--------|------|----------|-------------|
| `BMC_IP` | string | Yes | iDRAC/BMC IP address |
| `ADMIN_IP` | string | Yes | Node's admin network IP |
| `HOSTNAME` | string | Yes | Node hostname |
| `SERVICE_TAG` | string | No | Dell service tag |
| `FUNCTIONAL_GROUP_NAME` | string | No | Functional group assignment |

---

## 5. iso_config.yml (ARM Install)

**Purpose**: Configuration for AArch64 OS installation.

**Location**: `input/project_default/iso_config.yml`

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `iso_source_path` | string | Yes | - | Path to source RHEL ISO |
| `iso_source_checksum` | string | No | `""` | SHA256 checksum for verification |
| `iso_target_directory` | string | No | `<OMNIA_DATA_PATH>/iso_output` | Output directory |
| `nfs_share_path` | string | No | Auto-detect | NFS share for iDRAC virtual media |
| `kickstart_template` | string | No | `rhel10` | Built-in kickstart template |
| `kickstart_file` | string | No | `""` | Custom kickstart file path |
| `force_reinstall` | bool | No | `false` | Reinstall even if node is reachable |

---

## 6. Environment Variables

**Purpose**: System-wide configuration from `omnia.env`.

**Location**: `/etc/omnia/omnia.env` (installed by `omnia.sh --setup-venv`)

| Variable | Required | Description |
|----------|----------|-------------|
| `OMNIA_DATA_PATH` | Yes | Base path for Omnia data (default: `/opt/omnia`) |
| `OMNIA_PROJECT_NAME` | No | Project name (default: `project_default`) |
| `SYSTEM_ADMIN_NIC_IPV4` | Yes | OIM admin network IP |
| `SYSTEM_HOSTNAME` | Yes | OIM short hostname |
| `SYSTEM_DOMAIN_NAME` | No | Domain name |

---

## 7. Dependency Diagram

```
set_pxe_boot_config.yml     Inventory
+--------------------+      +------------------+
| enable_phone_home  |      | [bmc] hosts      |
| phone_home_*       |      | admin_ip         |
| restart_host       |      | hostname         |
+---------+----------+      +--------+---------+
          |                          |
          +------------+-------------+
                       |
               set_pxe_boot.yml
                       |
          +------------+-------------+
          |                          |
  pxe_mapping_file.csv    set_pxe_boot_credentials.yml
  (optional)              (vault-encrypted)
```
