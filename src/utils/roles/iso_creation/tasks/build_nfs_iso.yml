# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

# Build custom ISO using xorriso streaming (only modifies grub.cfg)
# Kickstart is hosted on NFS, referenced via inst.ks= in GRUB

- name: Render GRUB2 configuration with NFS kickstart reference
  ansible.builtin.template:
    src: grub_nfs.cfg.j2
    dest: "{{ temp_grub_cfg }}"
    mode: "0644"
  vars:
    nfs_server_ip: "{{ _nfs_server }}"
    nfs_kickstart_path: "{{ _nfs_dir }}/{{ nfs_kickstart_filename }}"

- name: Remove existing output ISO before rebuild
  ansible.builtin.file:
    path: "{{ _local_iso_path }}"
    state: absent

- name: Build custom ISO using xorriso (stream modification — grub.cfg only)
  ansible.builtin.command: >-
    xorriso -indev {{ source_iso_path }}
            -outdev {{ _local_iso_path }}
            -map {{ temp_grub_cfg }} /EFI/BOOT/grub.cfg
            -boot_image any replay
  changed_when: true
  register: _iso_build_result

- name: Remove temporary grub.cfg
  ansible.builtin.file:
    path: "{{ temp_grub_cfg }}"
    state: absent

- name: Implant MD5 checksum into custom ISO
  ansible.builtin.command: "implantisomd5 {{ _local_iso_path }}"
  changed_when: true
  failed_when: false

- name: Compute SHA-256 checksum of custom ISO
  ansible.builtin.stat:
    path: "{{ _local_iso_path }}"
    checksum_algorithm: sha256
  register: _custom_iso_stat_final

- name: Generate install manifest
  ansible.builtin.copy:
    content: |
      ---
      # Install OS Manifest
      # Generated by omnia.utils install_os (NFS-hosted kickstart)
      source_iso: "{{ source_iso_path }}"
      source_iso_checksum: "{{ source_iso_checksum | default('not provided') }}"
      custom_iso: "{{ custom_iso_path }}"
      custom_iso_checksum: "{{ _custom_iso_stat_final.stat.checksum }}"
      custom_iso_size: "{{ _custom_iso_stat_final.stat.size }}"
      kickstart_location: "nfs:{{ _nfs_server }}:{{ _nfs_dir }}/{{ nfs_kickstart_filename }}"
      kickstart_template: "{{ kickstart_template | default('user-provided') }}"
      build_method: "xorriso_streaming_grub_only"
      architecture: "{{ os_arch }}"
      timestamp: "{{ ansible_date_time.iso8601 | default(lookup('pipe', 'date -u +%Y-%m-%dT%H:%M:%SZ')) }}"
    dest: "{{ _local_manifest_path }}"
    mode: "0644"

- name: Log ISO build success
  ansible.builtin.debug:
    msg: >-
      Custom ISO built successfully (NFS kickstart).
      Output: {{ _local_iso_path }}
      (SHA-256: {{ _custom_iso_stat_final.stat.checksum }}).
      Kickstart: nfs:{{ _nfs_server }}:{{ _nfs_dir }}/{{ nfs_kickstart_filename }}.
      Manifest: {{ _local_manifest_path }}
