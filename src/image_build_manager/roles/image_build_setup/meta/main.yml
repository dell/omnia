---
galaxy_info:
  author: Dell Omnia Team
  description: Setup role for image build — loads config, validates environment, checks prerequisites
  license: Apache-2.0
  min_ansible_version: "2.20"

dependencies: []
