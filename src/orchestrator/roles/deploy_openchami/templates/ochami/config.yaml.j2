clusters:
  - cluster:
      uri: {{ ochami_base_url }}
      boot-service:
        uri: /boot-service
    name: {{ cluster_name }}
default-cluster: {{ cluster_name }}
log:
  format: {{ ochami_cli_log_format }}
  level: {{ ochami_cli_log_level }}
