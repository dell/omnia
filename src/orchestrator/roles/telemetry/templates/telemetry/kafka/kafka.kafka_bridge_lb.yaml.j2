---
apiVersion: v1
kind: Service
metadata:
  name: bridge-bridge-lb
  namespace: telemetry
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kafka-bridge
    app.kubernetes.io/instance: bridge
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
