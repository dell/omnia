# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

# Paths — resolved from environment variables at runtime (see tasks/main.yml)
# These defaults are overridden by set_fact in the setup tasks.
omnia_metadata_file_path: "{{ omnia_data_path | default('/opt/omnia') }}/.data/oim_metadata.yml"
upgrade_lock_file: "{{ omnia_data_path | default('/opt/omnia') }}/.data/upgrade_in_progress.lock"
default_build_status_path: >-
  {{ omnia_data_path | default('/opt/omnia') }}/image_build_manager/output/{{ project_name | default('project_default') }}/build_status.yml
default_repo_status_path: "{{ omnia_data_path | default('/opt/omnia') }}/repo_manager/output/{{ project_name | default('project_default') }}/repo_status.yml"
default_pxe_mapping_file_path: >-
  {{ omnia_data_path | default('/opt/omnia') }}/orchestrator/input/{{ project_name | default('project_default') }}/pxe_mapping_file.csv
default_catalog_file_path: "{{ lookup('env', 'CATALOG_FILE_PATH') | default(omnia_data_path | default('/opt/omnia') + '/catalog/catalog_rhel.json', true) }}"

# --- Tag validation ---
# Tags accepted by orchestrator.yml. Component-level cleanup tags (slurm,
# k8s, openchami, openldap, storage_mounts, artifacts) are intentionally
# absent: they are only valid when running
# playbooks/cleanup/cleanup_orchestrator.yml directly.
supported_tags:
  - precheck
  - validate
  - credentials
  - prepare
  - deploy
  - provision
  - execute
  - validate-deployment
  - pxeboot
  - cleanup
  - cleanup_credentials
  - upgrade
  - rollback

skip_credential_tags:
  - precheck
  - cleanup
  - cleanup_credentials
  - validate-deployment
  - validate

# cleanup + cleanup_credentials is a valid pair (clean everything including
# credential files); every other cleanup pairing is rejected.
invalid_tag_combinations:
  - [precheck, cleanup]
  - [validate, cleanup]
  - [validate-deployment, cleanup]
  - [credentials, cleanup]
  - [prepare, cleanup]
  - [deploy, cleanup]
  - [provision, cleanup]
  - [execute, cleanup]
  - [pxeboot, cleanup]
  - [cleanup, upgrade]
  - [cleanup, rollback]
  - [precheck, cleanup_credentials]
  - [validate, cleanup_credentials]
  - [validate-deployment, cleanup_credentials]
  - [credentials, cleanup_credentials]
  - [prepare, cleanup_credentials]
  - [deploy, cleanup_credentials]
  - [provision, cleanup_credentials]
  - [execute, cleanup_credentials]
  - [pxeboot, cleanup_credentials]
  - [cleanup_credentials, upgrade]
  - [cleanup_credentials, rollback]
  - [precheck, upgrade]
  - [validate, upgrade]
  - [validate-deployment, upgrade]
  - [credentials, upgrade]
  - [prepare, upgrade]
  - [deploy, upgrade]
  - [provision, upgrade]
  - [execute, upgrade]
  - [upgrade, rollback]

# OIM host group
oim_host_port: "22"
oim_host_group: "oim"

# Error messages
upgrade_in_progress_msg: >-
  Upgrade is not completed fully.
  Please run upgrade.yml to complete upgrade before running any other playbook using the below command:
  "ansible-playbook /omnia/src/playbooks/upgrade/upgrade.yml"
  If you don't require input files to be migrated, reconfigure the default input files, remove the lock file using the following command
  "rm {{ upgrade_lock_file }}" and then proceed.
