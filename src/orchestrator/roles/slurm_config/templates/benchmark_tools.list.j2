# Benchmark Tools List
# Lists HPC benchmark tools to pull from the local offline repository to /hpc_tools/<tool>/
# Format: <tool_name>  (one per line)
# Lines starting with # are ignored. Empty lines are ignored.
# Architecture is auto-detected at runtime (uname -m).
# Note: msr-safe is x86_64 only — automatically skipped on aarch64.

osu-micro-benchmarks
imb
likwid
papi
geopm
sionlib
msr-safe
