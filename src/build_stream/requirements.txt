# =============================================================================
# Build Stream — Python Requirements
# =============================================================================
# Install into the shared Omnia venv:
#   ./src/main/omnia.sh --setup-venv
# Or manually:
#   pip install -r requirements.txt
#
# NOTE: FastAPI / web-service dependencies (fastapi, uvicorn, sqlalchemy,
#       alembic, etc.) are managed separately via pyproject.toml + uv.lock
#       in containers/omnia_build_stream/ and installed into the container
#       image at build time. Only shared-venv dependencies needed by
#       Ansible playbooks, custom modules, and the playbook watcher are
#       listed here.
# =============================================================================

# --- Core Ansible ---
ansible-core>=2.20

# --- Python Libraries ---
PyYAML>=6.0.3
jsonschema>=4.17
cryptography>=48.0.0
